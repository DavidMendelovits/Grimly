#include "grimly.h"
#include "get_next_line.h"

int     make_adj_list(char ***graph, t_mapkeys *keys, char **map)
{
    int     i;
    int     j;



}

int     create_graph(t_graph *graph, char **map, t_mapkeys *keys)
{
    graph = (t_graph *)malloc(sizeof(t_graph));
    
}
